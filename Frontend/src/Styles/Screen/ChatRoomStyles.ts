import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  plus: {
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: 20,
  },
  delete: {
    color: 'white',
    fontSize: 16,
    fontWeight: 'bold',
    padding: 35,
    marginRight: -50,
    backgroundColor: 'red',
    justifyContent: 'center',
    alignSelf: 'center',
  },
});
